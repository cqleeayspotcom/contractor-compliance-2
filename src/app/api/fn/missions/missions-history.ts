/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ContractorMissionListResponse } from '../../models/contractor-mission-list-response';

export interface MissionsHistory$Params {
  status?: string;
  search?: string;
  invoice_status?: string;
  page?: number;
  per_page?: number;
}

export function missionsHistory(http: HttpClient, rootUrl: string, params?: MissionsHistory$Params, context?: HttpContext): Observable<StrictHttpResponse<ContractorMissionListResponse>> {
  const rb = new RequestBuilder(rootUrl, missionsHistory.PATH, 'get');
  if (params) {
    rb.query('status', params.status, {});
    rb.query('search', params.search, {});
    rb.query('invoice_status', params.invoice_status, {});
    rb.query('page', params.page, {});
    rb.query('per_page', params.per_page, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ContractorMissionListResponse>;
    })
  );
}

missionsHistory.PATH = '/contractor-compliance/missions/history';
